import stream_graph_py
import inspect

# print(dir(stream_graph_py))
# classes = [m[0] for m in inspect.getmembers(stream_graph_py, inspect.isclass)]
# print(classes)
# print(stream_graph_py.test_func(7))
# graph = stream_graph_py.Custom("aa", "BBBB")
# print(graph.name())

# import stream_graph_py

# Create an instance of Custom
custom_instance = stream_graph_py.Custom()

# Now you can use custom_instance as an object of the Custom type